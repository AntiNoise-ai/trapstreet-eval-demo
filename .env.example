# Copy to .env and fill in. Only the providers you use need keys.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional — used only by the LLM-judge grader fallback.
# Defaults to Anthropic's claude-haiku-4-5 if available.
ANTINOISE_JUDGE_MODEL=claude-haiku-4-5
